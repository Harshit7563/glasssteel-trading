{
  "name": "glassteel-trading",
  "version": "1.0.0",
  "private": true,
  "description": "GLASSTEEL TRADING (OPC) PRIVATE LIMITED — Building & home interiors, steel & antique glass",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "db:setup": "npm run db:setup --prefix server",
    "dev": "concurrently -k -n server,client -c cyan,magenta \"npm run start --prefix server\" \"npm run dev --prefix client\"",
    "dev:watch": "concurrently -k -n server,client -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run start --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
